<nav class="navbar navbar-expand-lg navbar-light bg-white bg-opacity-50 shadow-sm fixed-top">
  <div class="container">
    <a data-turbolinks="false" class="navbar-brand fw-bolder" href="/">
      <span class="px-2 p-1 bg-pink rounded text-white shadow-sm">Learn</span>
      Programming
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <% if user_signed_in? %>
            <a data-turbolinks="false" class="nav-link" href="/worklogs"> Time Logs </a>
          <% end %>
        </li>
      </ul>

      <ul class="navbar-nav mb-2 mb-lg-0 float-end">
        <% if user_masquerade? %>
          <li class="nav-item me-3">
            <%= link_to "Reverse masquerade", back_masquerade_path(current_user), class: 'nav-link', 'data-turbolinks' => 'false' %>
          </li>
        <% end %>
        <% if user_signed_in? %>
          <% if user_is_admin? %>
            <li class="nav-item me-1">
              <a data-turbolinks="false" class="nav-link" href="/admin"> Admin </a>
            </li>
          <% end %>
          <div class="btn-group">
            <a class="text-pink nav-link dropdown-toggle" data-bs-toggle="dropdown" data-bs-display="static" aria-expanded="false">
              <%= current_user.email %>
            </a>
            <ul class="dropdown-menu shadow-sm dropdown-menu-end dropdown-menu-lg-start">
              <li>
                <%= link_to "Sign out", destroy_user_session_path, method: :delete, data: { confirm: "Are you sure?" }, class: "dropdown-item", 'data-turbolinks': "false" %>
              </li>
            </ul>
          </div>
        <% else %>
          <li class="nav-item me-3">
            <a data-turbolinks="false" class="nav-link" href="/users/sign_in"> Log in </a>
          </li>

          <li class="nav-item">
            <a data-turbolinks="false" class="btn btn-pink shadow-sm" href="/users/sign_up"> Get Started </a>
          </li>
        <% end %>
      </ul>
      <button class="btn bg-light  bg-opacity-10 rounded-circle ms-3" id="modeToggler" onclick="toggleDarkMode()">🌑</button>
    </div>
  </div>
</nav>