<div class="container d-flex my-5 px-5">
  <div class="border bg-white col-3 shadow-sm px-3 py-2 pb-3">
    <p class="fs-3">Name:</p>
    <%= @project.name %>
  </div>

  <div class="border bg-white col-8 shadow-sm ms-auto px-3 py-2 pb-3">
    <p class="fs-3">Description:</p>
    <%= @project.description %>
  </div>
</div>

<div class="text-center">
  <%= link_to 'Back', admin_projects_path, class: 'text-pink me-3' %>
  <%= link_to 'Edit', edit_admin_project_path(@project), class: 'btn btn-pink shadow-sm' %>
</div>